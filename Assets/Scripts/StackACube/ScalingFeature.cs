using UnityEngine;

namespace StackACube
{
    public class ScalingFeature : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
            
        }

        public void PrintLog()
        {
            Debug.Log("Cube Scaling on");
        }
    }
}
